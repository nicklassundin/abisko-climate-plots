<script type="text/javascript">
	var addr = window.location.origin + window.location.pathname.replace('/index.html','')
	var q = queryString.parse(location.search);
	var type = q.id ? q.id : document.getElementById("type").value;
	var station = q.station ? q.station : document.getElementById("station").value;
	lib.renderSets(document.getElementById("mark"),
		type,
	station, addr);
</script>
