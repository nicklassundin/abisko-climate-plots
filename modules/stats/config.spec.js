const fs = require('fs')
const request = require('request')

const expect = require('chai').expect

const stats = require('./config.js')

describe('statistic config module', function () {
		
	it('[DUMMY]', function * () {
		// var result = stats.struct.create();
		// expect(result).to.eql(stats.struct);
	})
})
